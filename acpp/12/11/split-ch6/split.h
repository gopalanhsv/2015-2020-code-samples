#ifndef _SPLIT_H_
#define _SPLIT_H_

// System includes
#include "../lib/Str.h"
#include "../lib/Vec.h"

// Local includes

// Splits an input string into its contituent words
Vec<Str> split(const Str&);

#endif /* _SPLIT_H_ */

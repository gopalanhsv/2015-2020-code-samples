#ifndef _SPLIT_H_
#define _SPLIT_H_

// System includes
#include <string>
#include <vector>

// Local includes

// Splits an input string into its contituent words
std::vector<std::string> split(const std::string&);

#endif /* _SPLIT_H_ */

#ifndef _SYMMETRIC_PAIRS_H_
#define _SYMMETRIC_PAIRS_H_

// System headers
#include <vector>
#include <string>
#include <utility>

// Local includes

// Using declarations

// Statics, globals, typedefs

std::string
symmetricPairsInVec(const std::vector<std::pair<int, int> >& vp);

#endif /* _SYMMETRIC_PAIRS_H_ */

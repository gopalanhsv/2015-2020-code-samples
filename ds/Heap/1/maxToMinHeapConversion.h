#ifndef _CONVERT_MAX_TO_MIN_HEAP_H_
#define _CONVERT_MAX_TO_MIN_HEAP_H_

// System headers
#include <vector>

// Local includes

// Using declarations

// Statics, globals, typedefs

std::vector<int>
convertMaxHeapVecToMinHeap(const std::vector<int>& maxHeap);

#endif /* _CONVERT_MAX_TO_MIN_HEAP_H_ */

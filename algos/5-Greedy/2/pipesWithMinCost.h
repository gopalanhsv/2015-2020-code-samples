#ifndef _PIPES_WITH_MIN_COST_H_
#define _PIPES_WITH_MIN_COST_H_

// System headers
#include <vector>

// Local includes

// Using declarations

// Typedefs, statics, globals

int
connectPipesWithMinCostUsingBruteForce(std::vector<int> pipeCost);

int
connectPipesWithMinCostUsingMinHeap(std::vector<int> pipeCost);

int
connectPipesWithMinCostUsingPriorityQ(std::vector<int> pipeCost);

#endif /* _PIPES_WITH_MIN_COST_H_ */

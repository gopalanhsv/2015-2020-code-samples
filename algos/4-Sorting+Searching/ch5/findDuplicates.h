#ifndef _FIND_DUPLICATES_H_
#define _FIND_DUPLICATES_H_

// System headers
#include <vector>

// Local includes

// Using declarations

// Statics, globals, typedefs

std::vector<int>
findDuplicates(const std::vector<int>& v);

#endif /* _FIND_DUPLICATES_H_ */

#ifndef _SEARCH_STRING_IN_SPARSE_ARRAY_H_
#define _SEARCH_STRING_IN_SPARSE_ARRAY_H_

// System headers
#include <vector>
#include <string>

// Local includes

// Using declarations

// Statics, typedefs, globals

bool
searchStringInSparseArray(
    const std::vector<std::string>& v, const std::string& key);

#endif /* _SEARCH_STRING_IN_SPARSE_ARRAY_H_ */

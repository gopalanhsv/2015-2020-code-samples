#ifndef _FIND_INSERT_LOCATION_IN_SORTED_ARRAY_H_
#define _FIND_INSERT_LOCATION_IN_SORTED_ARRAY_H_

// System headers
#include <vector>

// Local includes

// Using declarations

// Statics, typedefs, globals

std::vector<int>::size_type
findInsertPosition(const std::vector<int>& v, int key);

#endif /* _FIND_INSERT_LOCATION_IN_SORTED_ARRAY_H_ */

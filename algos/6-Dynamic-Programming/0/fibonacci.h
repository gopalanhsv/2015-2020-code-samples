#ifndef _FIBONACCI_H_
#define _FIBONACCI_H_

// System headers

// Local includes

// Using declarations

// Statics, typedefs, globals

long long fibRecursive(long n);

long long fibRecursiveDPMemoization(long n);

long long fibDPTablulation(long n);

long long fibDPOptimized(long n);

#endif /* _FIBONACCI_H_ */

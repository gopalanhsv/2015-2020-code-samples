#ifndef _STAIRCASE_H_
#define _STAIRCASE_H_

// System headers

// Local includes

// Using declarations

// Statics, typedefs, globals

long long waysToClimbRecursive(long n);

long long waysToClimbRecursiveDPMemoization(long n);

long long waysToClimbDPTablulation(long n);

long long waysToClimbDPOptimized(long n);

#endif /* _STAIRCASE_H_ */

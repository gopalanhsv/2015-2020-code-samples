#ifndef _MINIMUM_WINDOW_SORT_H_
#define _MINIMUM_WINDOW_SORT_H_

// System headers
#include <vector>

// Using declarations

// Statics, typedefs, globals

class MinWindowSort {
public:
    static std::vector<int>::size_type
        minSubarrayLen(const std::vector<int>& v);
private:
};

#endif /* _MINIMUM_WINDOW_SORT_H_ */

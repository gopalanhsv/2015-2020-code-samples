#ifndef _MAX_WATER_VOLUME_H_
#define _MAX_WATER_VOLUME_H_

// System headers
#include <vector>

// Local includes

// Using declarations

// Statics, typedefs, globals

class MaxWaterVolume {
public:
    static unsigned long compute(const std::vector<int>& heightsV);
private:
};

#endif /* _MAX_WATER_VOLUME_H_ */

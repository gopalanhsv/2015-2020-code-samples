#ifndef _ALIEN_DICT_H_
#define _ALIEN_DICT_H_

// System headers
#include <vector>
#include <string>

// Local includes
// Using declarations
// Statics, typedefs, globals

class AlienDict {
public:
    static std::vector<char>
        dictionaryOrder(const std::vector<std::string>& wordV);
private:
};

#endif /* _ALIEN_DICT_H_ */

#ifndef _STRING_PERMUTATIONS_H_
#define _STRING_PERMUTATIONS_H_

// System headers
#include <string>
#include <vector>

// Local includes

// Using declarations
// Statics, typedefs, globals

class String {
public:
    static std::vector<std::string>
        generateAllLetterCasePermutations(const std::string& s);
private:
};

#endif /* _STRING_PERMUTATIONS_H_ */

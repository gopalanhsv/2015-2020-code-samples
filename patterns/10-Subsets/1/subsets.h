#ifndef _SUBSET_H_
#define _SUBSET_H_

// System headers
#include <vector>

// Local includes
// Using declarations
// Statics, typedefs, globals

class Subset {
public:
    static std::vector<std::vector<int> >
        determineSubsets(const std::vector<int>& v);
private:
};

#endif /* _SUBSET_H_ */

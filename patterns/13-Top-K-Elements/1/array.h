#ifndef _ARRAY_H_
#define _ARRAY_H_

// System headers
#include <vector>

// Local includes
// Using declarations
// Typedefs, statics, globals

class Array {
public:
    static std::vector<int>
    kLargestNumbers(const std::vector<int>& v, unsigned long k);
private:
};

#endif /* _ARRAY_H_ */

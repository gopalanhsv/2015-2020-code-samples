#ifndef _ARRAY_H_
#define _ARRAY_H_

// System includes
#include <vector>

// Local includes
// Using declarations
// Statics, typedefs, globals

class Array {
public:
    static std::vector<int>
    topKFrequentNumbers(const std::vector<int>& v, unsigned long k);
private:
};

#endif /* _ARRAY_H_ */

#ifndef _ARRAY_H_
#define _ARRAY_H_

// System headers
#include <vector>

// Local includes
// Using declarations
// Statics, typedefs, globals

class Array {
public:
    static unsigned int
    maxDistinctElementsPostRemoval(
        const std::vector<int>& v, unsigned int nRemove);
private:
};

#endif /* _ARRAY_H_ */

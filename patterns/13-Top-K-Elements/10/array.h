#ifndef _ARRAY_H_
#define _ARRAY_H_

// System headers
#include <vector>

// Local includes
// Using declarations
// Statics, typedefs, globals

class Array {
public:
    static unsigned long
    elementsSumBetweenK1AndK2SmallestElems(
        const std::vector<int>& v, unsigned long k1, unsigned long k2);
private:
};

#endif /* _ARRAY_H_ */

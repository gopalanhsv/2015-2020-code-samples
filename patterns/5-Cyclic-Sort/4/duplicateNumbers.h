#ifndef _DUPLICATE_NUMBER_H_
#define _DUPLICATE_NUMBER_H_

// System headers
#include <vector>

// Local includes

// Using declarations

// Statics, typedefs, globals

class AllDupNumbers {
public:
    static std::vector<int> findDuplicateNums(std::vector<int> v);
private:
};

#endif /* _DUPLICATE_NUMBER_H_ */

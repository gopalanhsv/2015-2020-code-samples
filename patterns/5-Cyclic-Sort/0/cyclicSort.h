#ifndef _CYCLIC_SORT_H_
#define _CYCLIC_SORT_H_

// System headers
#include <vector>

// Local includes

// Using declarations

// Statics, typedefs, globals

class CyclicSort {
public:
    static void sort(std::vector<int>& v);
private:
};

#endif /* _CYCLIC_SORT_H_ */

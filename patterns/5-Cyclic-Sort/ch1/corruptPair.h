#ifndef _CORRUPT_PAIR_H_
#define _CORRUPT_PAIR_H_

// System headers
#include <vector>

// Local includes

// Using declarations

// Statics, typedefs, globals

class CorruptPair {
public:
    static std::vector<int> findCorruptPair(std::vector<int> v);
private:
};

#endif /* _CORRUPT_PAIR_H_ */

#ifndef _DUPLICATE_NUMBER_H_
#define _DUPLICATE_NUMBER_H_

// System headers
#include <vector>

// Local includes

// Using declarations

// Statics, typedefs, globals

class DupNumber {
public:
    static int findDuplicateNum(std::vector<int>& v);
private:
};

#endif /* _DUPLICATE_NUMBER_H_ */
